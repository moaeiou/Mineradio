name: courir,partir,venir,mourir

on:
  push:
  workflow_dispatch:
permissions:
  contents: write
jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: "ubuntu-latest"
            tag: "linux:pacman"
            dickname: "dist/*.pacman"
          - os: "ubuntu-latest"
            tag: "linux:rpm"
            dickname: "dist/*.rpm"
          - os: "ubuntu-latest"
            tag: "linux:AppImage"
            dickname: "dist/*.AppImage"
          - os: "ubuntu-latest"
            tag: "linux:deb"
            dickname: "dist/*.deb"
          - os: "macos-latest"
            tag: "macos"
            dickname: "dist/*.dmg"
          - os: "windows-latest"
            tag: "windows"
            dickname: "dist/*.exe"
    runs-on: ${{ matrix.os }}
    name: Build for ${{ matrix.tag }}

    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Get version
        id: get_version
        shell: bash
        run: echo "version=$(node -p "require('./package.json').version")" >> "$GITHUB_OUTPUT"

      - name: Set up pnpm
        uses: pnpm/action-setup@master

      - name: Set up Node.js
        uses: actions/setup-node@main
        with:
          node-version: latest

      - name: Install Linux build dependencies
        if: matrix.os == 'ubuntu-latest'
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install libarchive-tools

      - name: Build!
        shell: bash
        run: |
          pnpm install
          pnpm build:${{ matrix.tag }} --publish never

      - name: Release
        uses: softprops/action-gh-release@master
        with:
          tag_name: V${{ steps.get_version.outputs.version }}
          files: ${{ matrix.dickname }}
          fail_on_unmatched_files: true
          body: |
            # 📝 Release Note
            - Fixes and improvements
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifact
        uses: actions/upload-artifact@main
        with:
          name: J'
          path: ${{ matrix.dickname }}

  sync:
    needs: build
    runs-os: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./jure
          merge-multiple: true

      - name: Send to mirror
        uses: moaeiou/moaeiou/actions/rsync@main
        with:
          local: ./jure/
          remote: /var/www/html/Mineradio/
          key: ${{ secrets.CC_LA_KEY }}
          extra: --delete
